import {
  TEXTEDIT_READY_CAPTURE_RESULT,
  type CaptureResult,
} from "./contracts";

const texteditReadyCaptureResult: CaptureResult = TEXTEDIT_READY_CAPTURE_RESULT;

void texteditReadyCaptureResult;
